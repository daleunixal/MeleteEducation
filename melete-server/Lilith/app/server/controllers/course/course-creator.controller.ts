import { ensuredRequest } from '../../../../utils/ensure-authenticated.middleware';
import { Response } from 'express';

export function createCourse(request: ensuredRequest, response: Response) {
}

export function createPart(request: ensuredRequest, response: Response) {
}
